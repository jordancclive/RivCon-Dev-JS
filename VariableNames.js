/*

Variable Names catch and hold values.

The name must:

        - start with a character.
        - can contain numbers in the name.
        - can only have the following special characters: '$' or '_'. 
        
If the variable is empty, then 'undefined' is the value.

*/
